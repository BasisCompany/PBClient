import { Box } from "@mui/material";
import { SecureDevices } from "./SecureDevices/SecureDevices";

export const SettingsSecure = () => {
    return (
        <Box sx={{ mt: 2 }}>
            <SecureDevices />
        </Box>
    );
};
